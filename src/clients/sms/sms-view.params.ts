interface SmsViewParams{
  /**
   * Start of search period as a unix timestamp.
   */
  unixStart: number;
  /**
   * End of search period as a unix timestamp.
   */
  unixEnd: number;
}
export {SmsViewParams}